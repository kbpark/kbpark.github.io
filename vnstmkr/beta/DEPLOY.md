# vnstmkr beta deploy

Target URL:

```text
https://kbpark.github.io/vnstmkr/
```

Upload the contents of this folder to the GitHub Pages publishing branch/folder for the `vnstmkr` repository.

Required files:

- `index.html`
- `css/`
- `js/`
- `assets/`
- `.nojekyll`

The app uses relative paths, so it works under the `/vnstmkr/` project-site path without changing asset URLs.
